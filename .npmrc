# pnpm v8 默认不运行 pre*/post* 生命周期钩子（v9+ 才默认启用）。
# 显式打开，让 component 的 prebuild / pretypecheck / pretest / predev
# 能在 `pnpm build` 等命令前自动跑 `lit-localize build` 生成 i18n 产物，
# 避免 CI 上因 `.generated/` 缺失导致构建失败。
enable-pre-post-scripts=true
